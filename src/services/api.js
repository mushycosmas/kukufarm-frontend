// API layer placeholder for future Laravel integration.
const api = {
  get: async () => ({ data: [] }),
  post: async (_, data) => ({ data }),
  put: async (_, data) => ({ data }),
  delete: async () => ({ data: null })
};
export default api;